#include <stdio.h>
#include <stdlib.h>
#include "lib.h"

unsigned char **__newArray(void){
  unsigned char **array = malloc(sizeof(char));
  array[0] = 0;
  return array;
}

//unsigned char __defineArray(unsigned char **array, int size, int arrayIsEmpty){

int main(void){

  unsigned char **a = __newArray();

  int var = -2;
  if(!var)
    puts("TEST");

  return 0;
}
