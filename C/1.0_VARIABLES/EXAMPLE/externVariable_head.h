#ifndef H_H
#define H_H

extern unsigned int e; // All files can use this variable.
// In fact, it's a global variable for all files. Warning
// the declarer [ extern ] is only in the file.h and there
// is not assignment with it.

void fct0(void);
void fct1(void);

#endif
